import 'package:flutter/material.dart';

class OpenStatus extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('opened detailes of status'),
    );
  }
}
